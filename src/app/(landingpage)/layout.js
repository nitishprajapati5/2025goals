export default function LandingPageLayout({children}){
    return(
        <main className="w-full min-h-screen bg-slate-100 lusitana-regular" style={{fontFamily:''}}>
        {children}
        </main>
    )
}